package lang.objectTest;

/**
 * 方法摘要
 *
 * clone()
 *
 * equals(Object obj)
 *
 * finalize()
 *
 * getClass()
 *
 * hashCode()
 *
 * notify()
 *
 * notifyAll()
 *
 * toString()
 *
 * wait()
 *
 * wait(long timeout)
 *
 * wait(long timeout,int nanos) 在其他线程调用此对象的 notify() 方法或 notifyAll() 方法，
 * 或者其他某个线程中断当前线程，或者已超过某个实际时间量前，导致当前线程等待。
 * timeout - 要等待的最长时间（以毫秒为单位）。
 * nanos - 额外时间（以毫微秒为单位，范围是 0-999999）。
 *
 *
 */
public class ObjectTest {


}
